import styled from 'styled-components';

const TaskCompletionStyles = styled.div`
  box-sizing: border-box;
  padding-left: 10px;
  padding-right: 10px;
  position: relative;

  .charts {
    position: absolute;
    left: 40px;
    top: -75px;
  }
`;

export default TaskCompletionStyles;

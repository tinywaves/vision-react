import styled from 'styled-components';

const ScrollRecordStyles = styled.div`
  box-sizing: border-box;
  padding-left: 10px;
  padding-right: 10px;
  margin-top: 7px;
`;

export default ScrollRecordStyles;
